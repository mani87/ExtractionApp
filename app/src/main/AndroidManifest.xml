<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.extractionapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FinalResultActivity"></activity>
        <activity android:name=".MidlineShiftActivity" />
        <activity android:name=".OverjetActivity" />
        <activity android:name=".ProclineTeethActivity" />
        <activity android:name=".NumberOfTeethActivity" />
        <activity android:name=".CrowdingActivity" />
        <activity android:name=".NasolabialAngleActivity" />
        <activity android:name=".PermanentTeethActivity" />
        <activity android:name=".BracesActivity" />
        <activity android:name=".GenderActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>